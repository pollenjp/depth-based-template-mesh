# depth-based-template-mesh

## requirements

- direnv

### 必要ファイルのダウンロードとPATHを設定

```sh
./setup.sh
direnv allow
```

### requirements.txt

```sh
python -m pip install -U poetry
python -m poetry install
```
